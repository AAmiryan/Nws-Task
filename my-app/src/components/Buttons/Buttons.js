import React from "react";
import "./Buttons.css";

const Button = () => {
  return (
    <div>
      <button type="button" className='button'>Click Me!</button>
    </div>
  );
};

export default Button;
